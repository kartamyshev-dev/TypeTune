[package]
name = "typetune-bridge"
version.workspace = true
edition.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
typetune-engine = { path = "../typetune-engine" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
