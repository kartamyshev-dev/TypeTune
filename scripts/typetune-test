#!/bin/sh
# Repository entry point; the installed IBus component runs independently.
TYPETUNE_ROOT=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
exec /usr/bin/python3 "$TYPETUNE_ROOT/integrations/ibus/controller.py" "$@"
